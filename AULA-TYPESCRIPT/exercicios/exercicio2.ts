let cidades: string[] = ['São Paulo', 'Mauá', 'Santo André', 'São Caetano', 'Brás'];

function listarCidades(cidades: string[]): void {
    cidades.forEach(cidades => console.log(cidades));
}

listarCidades(cidades);